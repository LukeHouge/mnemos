{
  "install": "bash scripts/setup-agent-env.sh",
  "start": "sudo service containerd start && sudo service docker start && timeout 30 sh -c 'until docker info >/dev/null 2>&1; do sleep 1; done'",
  "terminals": [
    {
      "name": "Compose stack",
      "command": "docker compose up postgres dev"
    }
  ],
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  }
}
